# Instructions

After instalation run each of this commands to create symlink and update prompt
```bash
ln -rs ~/.dotfiles/nvim ~/.config/nvim
ln -s ~/.dotfiles/.gitconfig ~/.gitconfig
echo "source ~/.fancy-bash-promt.sh" >> ~/.bashrc
```

Alternatively Run 
```bash
./install.sh
```
to do this for you